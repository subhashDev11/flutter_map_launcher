enum LaunchStatus {
  launching,
  loading,
  launched,
  error,
}

enum SnackbarPosition {
  top,
  center,
  bottom,
  topCenter,
  bottomCenter,
}

enum AppNotificationAction {
  Jobs,
  News,
  Alumni,
}

enum AppTheme {
  dark,
  light,
}
